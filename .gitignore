# Local Claude Code config & session handoff — this is a PUBLIC GitHub Pages
# repo, so these must never be committed/pushed (they'd be served at treforged.com).
.claude/
handoff.md
backups/

# Dev tooling only — the linter's dependencies. Nothing here is served: the
# site has no build step, so node_modules exists purely so `npm run lint` works.
node_modules/
